# DW2 game data / extracted or converted assets — never commit Slitherine/Matrix Games IP
/test-workspace/
*.bundle
*.fbx
*.wav
*.ogg
*.dds
*.png
*.sdsl
*.sdfx

# Local machine configuration (points at your own DW2 install; not portable)
DW2BT.local.props

# .NET / Visual Studio build output
[Bb]in/
[Oo]bj/
.vs/
*.user

# Published build output — bundles the real Xenko/Stride/DistantWorlds2 engine
# DLLs alongside dw2extract.exe, which can't be redistributed (Slitherine/Matrix
# IP, not a redistributable SDK). Build it yourself via `dotnet publish` per
# DW2BT Source/README.md.
/dw2-asset-extractor/

# Claude Code project context — internal dev notes, not for public/user consumption
/CLAUDE.md
/.claude/
